{
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  },
  "type": "module",
    "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "migrar": "node --env-file=.env src/migrar.js",
    "test": "vitest run"
  }
}
